data is already cleaned
i have a csv file which i converted into the xlsx file

i imported that file to the powerbi now 
no i have to clean it , the csv contains of only one table and 11 columns and 9216 rows.

saw it in power query edior only the patient satisfaction score
has null values rest all data is nice 100 filled without any error or empty

addeda new column using patients first name and last name

changed m to male, f to female and nc to not confirmed


close nd aplli

back to powerbi 
add new table called data table , added date monthname and year columns in it

now connected the two tables, made a relationship between them using date column.

started doing the first page of the dashboard, monthlyoverview- added shapes, no. of patients visula cards, added two slicers for year and month name, but it showed blank in the patients cards so there is some error, find that error
error : BUT the patient admission time has two fields date as wellas a time stamp , so when i connected both tables
it didnot get get connnected correctly as the date tabke tabke had wrong timestamps and didnot match the timestamps from patient table.

so i first amde new co